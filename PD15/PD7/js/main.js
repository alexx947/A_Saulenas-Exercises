const names = ['James', 'Adrian', 'Nala', 'Rodriguez'];

const uppercased = names.map(name => name.toUpperCase());
const lowercased = names.map(name => name.toLowerCase());

console.log(uppercased);
console.log(lowercased);