const buttonBackground = document.getElementById("changeBackground");

buttonBackground.addEventListener("click", function () {
    const staticColor = "purple";
    document.body.style.backgroundColor = staticColor;
});